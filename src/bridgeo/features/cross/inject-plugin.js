import { lifecycle } from "../../utils/js/plugins.js";
import VersionRelay from "./version/version-relay.js";

export function onEnabled() {
    // lifecycle.on('relay.creating', context => context.class = VersionRelay);
}
